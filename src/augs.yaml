# augmentations are mostly taken from Albumentations with default values

# Objects (foregrounds) augmentations
objects:
  count: [0, 5] # number of objects (range) per image for every class
  augs:
    color:
      RandomBrightnessContrast: true
      RandomGamma: true
      HueSaturationValue: true
      Blur: false
    spacial:
      # flip up-down
      VerticalFlip: false

      # flip left-right
      HorizontalFlip: true

      # flip the input either horizontally, vertically or both horizontally and vertically
      Flip: false

      RandomScale: [ 0.7, 1.5 ]
      #RandomScale: false # how to disable

      # use false to disable
      Rotate: [-90, 90]
      #Rotate: false # how to disable


# Background image augmentations (coming soon)
# background: false

# Result image augmentations  (coming soon)
# result: false